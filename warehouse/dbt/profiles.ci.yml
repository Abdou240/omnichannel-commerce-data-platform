omnichannel_platform:
  target: ci
  outputs:
    ci:
      type: duckdb
      path: "{{ env_var('WAREHOUSE_PATH') }}"
      threads: 4

# TODO:
# - mirror any future macros, vars, or adapter-specific settings that become required in CI
# - keep CI focused on scaffold-safe validation until real sources are introduced

